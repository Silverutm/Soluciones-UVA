from math import factorial
num = [1]*500
n = int(input())
while n:        
    print (str(n) + '! --')
    x = str( factorial(n) )
    for c in x:
        num[x] += 1
    for w in range(0,10)
        print (num[w+'0'])
    n = int(input())
    