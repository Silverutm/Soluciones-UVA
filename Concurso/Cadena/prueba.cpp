#include <bits/stdc++.h>
using namespace std;
set <string> mapa;
vector <string> total;
int main()
{
	string s, s1;
	getline(cin,s);
	while ( getline(cin,s, '\t') and getline(cin,s1) )
		if (s!="") {mapa.insert(s);total.push_back(s);}
	
	//for (auto &e:mapa)
	//	cout<<e<<"\n";
	//cout<<"\n\n\n";

	sort (total.begin(), total.end());
	//for (auto &e:total)
	//	cout<<e<<"\n";


	respuesta = cade

	//cout<<mapa.size()<<"\n";
}